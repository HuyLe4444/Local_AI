---
name: ai-response-generator
channels:
  - defaults
  - conda-forge
  - pytorch
dependencies:
  - python=3.10.13
  - cpuonly
  - pytorch=2.4.0
  - transformers
  - tqdm
  - tokenizers
  - pip
  - pip:
      - aiohttp==3.8.6
      - requests
